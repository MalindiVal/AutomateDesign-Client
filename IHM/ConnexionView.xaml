<UserControl x:Class="IHM.ConnexionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IHM"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Bouton principal -->
        <Style x:Key="PrimaryButtonLarge" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{DynamicResource App.Primary}" CornerRadius="12" Padding="2,2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour les TextBox -->
        <Style x:Key="InputTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource App.Primary}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style pour PasswordBox -->
        <Style x:Key="InputPasswordBox" TargetType="PasswordBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource App.Primary}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Padding="24" Background="{DynamicResource App.Surface}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Bouton Retour -->
            <Button Style="{StaticResource PrimaryButtonLarge}"
                    Content="Retour"
                    Grid.Row="0" Grid.Column="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="150"
                    Height="40"
                    Command="{Binding ReturnCommand}"/>

            <!-- Titre -->
            <Image Source="pack://application:,,,/images/AUTOMATEDESIGN.png"
                   Height="36" Stretch="Uniform"
                   VerticalAlignment="Top" 
                   Grid.Row="0" Grid.Column="1"/>

            <!-- Formulaire de connexion -->
            <Border Grid.Row="1" Grid.Column="1" 
                    Background="#D2D2D2" 
                    CornerRadius="12" 
                    Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Identifiant -->
                    <TextBlock Text="Identifiant" 
                               Grid.Row="0" 
                               FontWeight="Bold" 
                               FontSize="14"
                               Margin="0,0,0,4"/>
                    <TextBox x:Name="IdentifiantTextBox" 
                             Grid.Row="1" 
                             Style="{StaticResource InputTextBox}"
                             Text="{Binding Identifiant, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             TabIndex="0"/>

                    <!-- Mot de passe -->
                    <TextBlock Text="Mot de passe" 
                               Grid.Row="3" 
                               FontWeight="Bold" 
                               FontSize="14"
                               Margin="0,0,0,4"/>
                    <PasswordBox x:Name="PasswordBox" 
                                 Grid.Row="4"
                                 Style="{StaticResource InputPasswordBox}"
                                 PasswordChanged="PasswordBox_PasswordChanged"
                                 TabIndex="1"/>

                    <!-- Message d'erreur -->
                    <TextBlock Grid.Row="6" 
                               Text="{Binding ErrorMessage}"
                               Foreground="#D32F2F"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               HorizontalAlignment="Center"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                    <!-- Indicateur de chargement -->
                    <StackPanel Grid.Row="7" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Connexion en cours" 
                                   FontStyle="Italic" 
                                   Foreground="#666666"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <ProgressBar IsIndeterminate="True" 
                                     Width="100" 
                                     Height="4"/>
                    </StackPanel>

                    <!-- Bouton Connexion -->
                    <Button Style="{StaticResource PrimaryButtonLarge}"
                            Content="Connexion"
                            Grid.Row="8"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Width="150"
                            Height="40"
                            Command="{Binding ConnectCommand}"
                            TabIndex="2"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
<UserControl x:Class="IHM.ImportAutomateWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource App.Surface}">

    <UserControl.Resources>
        <!-- Style bouton primaire -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{DynamicResource App.Primary}"
                                CornerRadius="12"
                                Padding="16,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Sélectionnez un automate à importer"
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Automates}" 
                 SelectedItem="{Binding SelectedAutomate}"
                 DisplayMemberPath="Nom"
                 Background="White"
                 BorderBrush="#DDD"
                 BorderThickness="1"
                 FontSize="16"/>

        <TextBlock Grid.Row="3" 
Text="{Binding ErrorMessage}"
Foreground="#D32F2F"
FontWeight="SemiBold"
TextWrapping="Wrap"
HorizontalAlignment="Left"
Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Importer" Width="120" Margin="0,0,8,0" 
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding ImportCommand}"/>
            <Button Content="Annuler" Width="120" Style="{StaticResource PrimaryButton}"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>

    </Grid>
</UserControl>
